# golangci-lint configuration for cent-mem
run:
  timeout: 5m
  build-tags:
    - fts5

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
  disable:
    - exhaustruct

issues:
  exclude-use-default: false
